<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduClockPlus - Smart School Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
            background: #f8f9fa;
        }

        .container {
            position: relative;
            z-index: 2;
        }

        .bg-decoration {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
            overflow: hidden;
            pointer-events: none;
        }

        .floating-element {
            position: absolute;
            border-radius: 50%;
            opacity: 0.05;
            animation: float-around 20s infinite ease-in-out;
        }

        .floating-element:nth-child(1) {
            width: 300px;
            height: 300px;
            background: #0d6efd;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }

        .floating-element:nth-child(2) {
            width: 200px;
            height: 200px;
            background: #198754;
            top: 60%;
            right: 10%;
            animation-delay: 2s;
        }

        .floating-element:nth-child(3) {
            width: 150px;
            height: 150px;
            background: #ffc107;
            bottom: 10%;
            left: 15%;
            animation-delay: 4s;
        }

        @@keyframes float-around {
            0%, 100% {
                transform: translate(0, 0) scale(1);
            }
            33% {
                transform: translate(30px, -30px) scale(1.1);
            }
            66% {
                transform: translate(-20px, 20px) scale(0.9);
            }
        }

        .py-5 {
            animation: fadeIn 0.8s ease-out;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .display-4 {
            animation: slideInDown 0.8s ease-out;
            position: relative;
            display: inline-block;
        }

        @@keyframes slideInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .lead {
            animation: fadeIn 1s ease-out 0.3s both;
        }

        .btn {
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            font-weight: 600;
            border-radius: 50px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            box-shadow: 0 4px 15px rgba(13, 110, 253, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(13, 110, 253, 0.4);
        }

        .btn-outline-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(13, 110, 253, 0.2);
        }

        .d-flex {
            animation: fadeIn 1.2s ease-out 0.5s both;
        }

        .card {
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            animation: fadeInUp 0.8s ease-out both;
            border-radius: 20px;
            background: #ffffff;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(13, 110, 253, 0.05) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .card:hover::before {
            opacity: 1;
        }

        .col-md-3:nth-child(1) .card {
            animation-delay: 0.7s;
        }

        .col-md-3:nth-child(2) .card {
            animation-delay: 0.9s;
        }

        .col-md-3:nth-child(3) .card {
            animation-delay: 1.1s;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15) !important;
        }

        .card-body i {
            transition: all 0.3s ease;
            display: inline-block;
        }

        .card:hover .card-body i {
            transform: scale(1.2) rotate(5deg);
        }

        .card-body h5 {
            transition: color 0.3s ease;
        }

        .card:hover .card-body h5 {
            color: #0d6efd;
        }

        @@keyframes pulse-icon {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
        }

        .card-body i {
            animation: pulse-icon 2s ease-in-out infinite;
        }

        .card:nth-child(1) .card-body i {
            animation-delay: 0s;
        }

        .card:nth-child(2) .card-body i {
            animation-delay: 0.3s;
        }

        .card:nth-child(3) .card-body i {
            animation-delay: 0.6s;
        }

        .mt-4 {
            animation: fadeIn 1.5s ease-out 1.3s both;
        }

        @@media (max-width: 768px) {
            .display-4 {
                font-size: 2rem;
            }

            .lead {
                font-size: 1rem;
            }

            .d-flex {
                flex-direction: column;
                align-items: center;
            }

            .btn-lg {
                width: 100%;
                max-width: 300px;
                margin: 0 0 0.75rem 0 !important;
            }

            .btn-lg.me-3 {
                margin-right: 0 !important;
            }

            .card {
                margin-bottom: 1.5rem;
            }

            .floating-element {
                opacity: 0.03;
            }
        }

        @@media (max-width: 576px) {
            .container {
                padding-left: 1rem;
                padding-right: 1rem;
            }

            .display-4 {
                font-size: 1.75rem;
            }

            .card-body {
                padding: 1.5rem;
            }

            .card-body i {
                font-size: 2.5rem !important;
            }
        }

        /* Smooth scroll behavior */
        html {
            scroll-behavior: smooth;
        }

        /* Add sparkle effect on hover for title */
        .text-primary {
            position: relative;
            display: inline-block;
        }

        .display-4:hover .text-primary {
            animation: sparkle 0.6s ease;
        }

        @@keyframes sparkle {
            0%, 100% {
                text-shadow: none;
            }
            50% {
                text-shadow: 0 0 20px rgba(13, 110, 253, 0.6);
            }
        }

        /* Loading state for buttons */
        .btn:active {
            transform: scale(0.95);
        }
    </style>
    
</head>
<body>
    <div class="bg-decoration">
        <div class="floating-element"></div>
        <div class="floating-element"></div>
        <div class="floating-element"></div>
    </div>

    <div class="container py-5 text-center">

        <h1 class="display-4 fw-bold text-primary mb-3">Welcome to <span class="text-dark">EduClock<span class="text-primary">Plus</span></span></h1>
        <p class="lead text-muted mb-4">
            Empowering schools with smart digital tools — from attendance tracking and teacher management 
            to parent engagement and real-time performance insights.
        </p>

        <div class="d-flex justify-content-center mb-4">
            <a href="/Account/RegisterSchool" class="btn btn-primary btn-lg px-4 me-3">
                <i class="bi bi-building-add me-2"></i> Register Your School
            </a>
            <a href="/Account/Login" class="btn btn-outline-primary btn-lg px-4">
                <i class="bi bi-box-arrow-in-right me-2"></i> Login
            </a>
        </div>

        <div class="row justify-content-center mt-5">
            <div class="col-md-3 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <i class="bi bi-people display-5 text-primary mb-3"></i>
                        <h5 class="fw-bold">For Administrators</h5>
                        <p class="text-muted">Easily manage classes, teachers, and performance reports from a single dashboard.</p>
                    </div>
                </div>
            </div>

            <div class="col-md-3 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <i class="bi bi-person-workspace display-5 text-success mb-3"></i>
                        <h5 class="fw-bold">For Teachers</h5>
                        <p class="text-muted">Track attendance, assign grades, and communicate with parents seamlessly.</p>
                    </div>
                </div>
            </div>

            <div class="col-md-3 mb-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <i class="bi bi-chat-dots display-5 text-warning mb-3"></i>
                        <h5 class="fw-bold">For Parents</h5>
                        <p class="text-muted">Stay informed about your child's progress, school activities, and announcements.</p>
                    </div>
                </div>
            </div>
        </div>

        <p class="text-muted mt-4">
            <small>Trusted by educators and parents worldwide — EduClockPlus simplifies school management for everyone.</small>
        </p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>